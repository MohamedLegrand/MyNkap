# FastAPI et Configuration
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0
python-dotenv==1.2.2
python-multipart==0.0.32

# Base de données (PostgreSQL & ORM)
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0

# Sécurité & Authentification (JWT & Bcrypt)
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
slowapi==0.1.9

# Authentification "Se connecter avec Google" (vérification du jeton
# d'identité OAuth émis par Google Identity Services côté frontend).
# requests==2.34.2 : dépendance requise par google.auth.transport.requests
# (voir auth.services) — ne pas compter sur un paquet tiers (pyHanko...)
# pour l'installer par hasard, une version plus récente pourrait s'en passer
# et casser le démarrage de l'app (constaté en déploiement : ImportError).
google-auth==2.36.0
requests==2.34.2

# Tâches de fond (Celery & Redis)
celery==5.4.0
redis==5.2.1

# Paiement Mobile Money (module Plans/Abonnement). 1.0.0 (rupture de compat
# avec 0.1.x) : authentification renforcée côté serveur HR-Skills Pay —
# 0.1.0 échouait systématiquement l'échange de jeton (401 "invalid api
# secret") quelles que soient les clés fournies, corrigé par cette mise à
# jour. Signatures utilisées ici (HRPayClient, cash_in.mobile_money,
# transactions.status, .reference/.status, Status.SUCCESS/FAILED/...)
# inchangées, vérifié avant migration. Le SDK expose désormais aussi un
# module carte bancaire natif (sdk.cards / E-NKAP) — non adopté ici,
# app/core/hrpay_card.py (client HTTP maison) reste utilisé tel quel pour
# l'instant, la carte étant déjà en pause (clés test manquantes).
hrpay==1.0.0

# Client HTTP pour l'API "Encaissement par carte bancaire" d'E-NKAP :
# app/core/hrpay_card.py. Sert aussi aux tests (TestClient s'appuie sur
# httpx).
httpx==0.28.1

# Génération de rapports PDF (module Rapports)
xhtml2pdf==0.2.17
jinja2==3.1.6

# Tests et Validation (httpx : voir section paiement carte ci-dessus)
pytest==8.3.4
